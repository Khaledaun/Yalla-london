export const dynamic = 'force-dynamic';
export const revalidate = 0;

import PremiumAdminDashboard from '@/src/pages/admin/dashboard';

export default function AdminDashboardPage() {
  return <PremiumAdminDashboard />;
}
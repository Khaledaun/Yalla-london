# Admin Page Registry

> Generated by `/admin-rebuild` Audit Phase. Run `/admin-rebuild` to populate with real data.

## Classification Key

| Status | Meaning |
|--------|---------|
| KEEP | Actively used, unique functionality, linked from sidebar |
| MERGE | Overlapping with a canonical page — redirect to canonical, merge unique features |
| ARCHIVE | Not linked, no recent git activity, may have useful patterns |
| DELETE | Dead code, placeholder, mock-only, no references |
| REBUILD | Exists but broken, has mock data, needs real API wiring |

## Pages (run audit to populate)

*This file is auto-generated by the `/admin-rebuild` skill's Audit phase.*
*Run `/admin-rebuild` to scan all 132 admin pages and classify each one.*

## Known Consolidation Targets (pre-identified)

| Cluster | Current Pages | Canonical | Action |
|---------|--------------|-----------|--------|
| Affiliate | 15 pages | `affiliate-hq/` | Keep affiliate-hq (6-tab), delete rest |
| Content/Articles | 11 pages | `articles/` + `content-engine/` | Keep both canonical, merge duplicates |
| SEO | 6 pages | `seo/` (tabbed) | Keep seo with tabs, delete rest |
| Design | 4 pages | `design/` + `media/` | Keep both, merge design-studio into design |
| Cron | 5 pages | `departures/` | Keep departures, delete rest |
| Settings | 3+ pages | `settings/` + `operations/` | Keep both, delete command-center/settings |
